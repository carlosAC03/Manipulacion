const frase = "Aprender JavaScript es divertido";

const palabras = frase.split(" ");
console.log("Palabras usando split:", palabras);

const caracteres = Array.from(frase);
console.log("Caracteres usando Array.from:", caracteres);
